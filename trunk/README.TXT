Each project has its own root directory.  
E.g. The utility project is under /util.

The standard directory structure for projects is 
as follows:

project directory
    build files     : Ant
    test directory  : Test source.
    src directory   : Project source.
