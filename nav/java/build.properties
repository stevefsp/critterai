version=snapshot
project.name=cai-nav
project.fullname=${project.name}-${version}

lib.dir=../../lib/java
util.lib = ${lib.dir}/cai-util
util.version=0.1.1

misc.resources.dir=../../misc

src.dir=src
local.misc.dir=misc
local.resources.dir=resources
test.src.dir=test

build.dir=build

classes.dir=${build.dir}/classes
test.classes.dir=${build.dir}/test
dist.dir=${build.dir}/dist
doc.dir=${build.dir}/docs